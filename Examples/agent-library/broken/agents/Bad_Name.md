---
name: Bad_Name
description: An agent whose file name breaks the name rule.
---

The one defect of this file: the file name `Bad_Name` holds capital letters
and an underscore. The name rule accepts only 1 to 64 of `[a-z0-9-]`. The
loader skips the file.
